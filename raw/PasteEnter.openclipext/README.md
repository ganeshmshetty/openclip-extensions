# Paste Enter

Paste clipboard contents and automatically simulate pressing the Return key.

## Features

- **Paste and Submit**: Executes `Cmd+V` followed immediately by the `Return` key.
- **Form & Chat Accelerators**: Send chat messages or submit single-field forms with a single click.

## Requirements

- OpenClip Accessibility permissions enabled to post keyboard events.
