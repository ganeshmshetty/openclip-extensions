# Inline Code

Wrap selected text in Markdown backticks (`` `code` ``).

## Features

- **Code Formatting**: Wraps selected text in backticks.
- **Markdown Ready**: Ideal for highlighting variable names, CLI commands, and code terms.

## Usage Examples

- Select `npm run dev` → `` `npm run dev` ``
