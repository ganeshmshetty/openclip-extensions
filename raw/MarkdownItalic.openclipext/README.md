# Markdown Italic

Format selected text as Markdown italic (`*italic*`).

## Features

- **Markdown Formatting**: Wraps the selected text with single asterisks `*``.

## Usage Examples

- Select: `emphasis`
- Output: `*emphasis*`
