# Round Brackets

Wrap the selected text in round brackets (parentheses).

## Features

- **Quick Wrapping**: Surrounds selection with `(` and `)`.
- **Whitespace Preserving**: Keeps external whitespace intact while enclosing the selected content.

## Usage Examples

- Select `"example"` → Returns `"(example)"`.
